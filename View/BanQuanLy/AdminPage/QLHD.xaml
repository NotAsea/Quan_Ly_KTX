<Page x:Class="Quan_Ly_KTX.Main_Window.AdminPage.QLHD"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Quan_Ly_KTX.Main_Window.AdminPage"
      mc:Ignorable="d" 
       d:DesignHeight="450" d:DesignWidth="1000"
      Title="QLHD"
      Background="Bisque">

    <Page.Resources>
        <CollectionViewSource x:Key="HDList"/>
    </Page.Resources>
    <StackPanel>
        <TextBlock Text="Danh sách Hóa đơn" FontSize="30" FontWeight="Bold" Foreground="DarkMagenta" HorizontalAlignment="Center" Margin="0 10"/>
        <StackPanel Orientation="Horizontal" Margin="0 20" HorizontalAlignment="Center" >
            <TextBox x:Name="ElementtoFind" Width="300" Background="LightGreen" FontSize="18"  />
            <Button x:Name="Find" Click="Find_Click" Content="Tìm Kiếm " Background="CadetBlue" Margin="20 0" Padding="2" FontSize="18" FontFamily="Calibri"/>
            <Button x:Name="ThemHD" Click="ThemHD_Click" Content="Nhập hóa đơn" Background="DarkSalmon" Padding="2" FontSize="18" FontFamily="Calibri"/>
        </StackPanel>
        <Grid>
            <DataGrid ItemsSource="{Binding Source={StaticResource HDList}}" AutoGenerateColumns="False" IsSynchronizedWithCurrentItem="True" FontSize="18" FontFamily="Arial"
                      HorizontalAlignment="Center" Width="Auto" ScrollViewer.CanContentScroll="True"         
                              ScrollViewer.IsDeferredScrollingEnabled="True" 
                              ScrollViewer.VerticalScrollBarVisibility="Visible" Height="300" >
                <DataGrid.Columns>
                    
                    <DataGridTextColumn Header="Mã Phòng" Binding="{Binding Maphong}" IsReadOnly="True" Width="150"/>
                    <DataGridTextColumn Header="Mã sinh viên" Binding="{Binding MaSv}" IsReadOnly="True" Width="140"/>
                    <DataGridTextColumn Header="Tên sinh viên" Binding="{Binding TenSV}" IsReadOnly="True" Width="200"/>
                    <DataGridTextColumn Header="Dịch vụ chung" Binding="{Binding DichVuChung}" IsReadOnly="True" Width="150"/>
                    <DataGridTextColumn Header="Dịch vụ riêng" Binding="{Binding Dichvurieng}" IsReadOnly="True" Width="150"/>
                    <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TienRieng}" IsReadOnly="True" Width="100"/>
<!--
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button x:Name="Edit" Click="Edit_Click" Content="Edit" FontSize="18"  Padding="3" Background="ForestGreen" Width="50" Margin="20 0"/>
                            </DataTemplate>

                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button x:Name="Remove" Click="Remove_Click" Content="Xóa" FontSize="18" Padding="3" Background="Red" Width="50" Margin="20 0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    -->
                </DataGrid.Columns>

            </DataGrid>

        </Grid>
    </StackPanel>
</Page>
